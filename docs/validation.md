# 验证记录

版本：0.2.0。检查日期：2026-09-09 至 2026-09-10。

## 自动检查

- 后端：566 项测试通过，另有 12 项子测试通过；Ruff 检查和格式检查通过。测试工具有 2 条第三方弃用提示。
- 前端：13 个测试文件中的 173 项测试通过，TypeScript 检查与生产构建通过。
- 前端依赖审计：0 个已报告漏洞。
- 插件：Codex 插件清单、Skill 和 Claude Code 插件清单通过校验；隔离目录实际安装及文件摘要核对通过。
- 启动：真实本机启动、启动链接登录、端口占用拒绝及服务退出检查通过。

穿搭编辑回归覆盖包袋与配饰分类、标签搜索、8 件单品的完整展示与保存、四种方式切换、指针与键盘移动、缩放旋转及图层持久化。三种拼图模板均检查成员删除后新增的重排；自由布局检查手动变换保留。保存期间编辑锁定、失败后草稿恢复、推荐来源保留，以及推荐最多保留 12 件与手动最多 24 件的边界均通过测试。后端检查布局校验、创建与更新、旧记录兼容、备份恢复和删除单品后的布局清理。

直接缩放与旋转手柄通过鼠标和触摸指针测试，覆盖非零画布位置下的中心几何、缩放上下限、跨 180° 旋转、捕获释放与取消、忽略其他指针、键盘替代操作及保存期间禁用。右上边缘的 0°／45° 单品与画布尺寸变化均检查两个手柄保持可触及且互不覆盖。

## 功能检查

图片美化通过真实 Codex 内置图像工具完成一次白色 T 恤处理：助手领取任务、读取原图、生成单品展示图并回传预览；浏览器完成采用、恢复原图与再次采用，原图保留。图片 API 使用模拟服务验证请求格式、返回解码、错误、取消和配置切换，未使用真实收费 API 密钥调用。自动检查覆盖授权领取、租约、过期回传、配置与原图变更、删除后重建同编号、预览独立保存和完整备份恢复。

服饰属性检查覆盖手动创建与更新、旧数据兼容、备份恢复、API／Ollama／Codex 识别协议、未知属性保留与材质文字证据要求。尺码和洗护说明保持由用户填写。

在独立的测试衣橱中完成照片上传、真实 CPU 去背景、恢复处理前图片、手动属性确认、规则搭配、保存搭配、安排日期、记录与撤销实际穿着、整套加入旅行清单和勾选打包。

使用真实 HTTP 服务和独立助手命令行完成 27 项集成检查，覆盖一次性连接码、权限边界、衣物读取、图片下载、标签回写、组合校验及断开授权。图片检查使用 u2netp 与自行构造的衣物图形，处理图确实改变，恢复图与规范化原图逐字节相同。

助手连接完成 Windows 本机实测：网页安装个人技能、核对并批准短码、DPAPI 保存授权、独立命令重新读取凭据和衣柜。服务重启及技能更新后连接仍可用，安装文件摘要与源文件一致。配对测试覆盖批准、拒绝、一次性兑换、过期、限频、请求编号关联和当前授权撤销；CLI 测试覆盖失败回滚、并发文件锁及重连后旧授权清理。技能升级备份保存在技能搜索目录外。

真实 Codex 图片识别完成 Windows 本机验收，使用已登录的 Codex CLI 0.153.4。在独立衣橱通过浏览器上传自行绘制的蓝色 T 恤图片，完成 CPU 去背景、自动识别、七字段返回、待确认表单填充和确认保存。品牌保持空白，购买价格保持未填；识别期间编辑的名称被保留，刷新后仍正确。此测试验证了接入与流程，不代表复杂服装照片的识别准确率评估。

OpenAI、兼容 API 和 Ollama 使用可控的模型响应分别验证默认自动识别、逐次跳过、完整字段回填、缺少视觉模型、模型拒绝图片、配置切换和任务取消。未配置真实云端密钥或运行本地视觉模型服务，未进行这三类服务的真实模型质量评估。Codex 子进程测试覆盖格式校验、总调用超时、取消、串行执行及临时文件清理。

商品链接完成真实公开页面与图片读取，京东三例、淘宝公开详情两例、拼多多一例和得物分享短链一例成功返回可解码商品图；淘宝／天猫普通详情样本返回登录跳转，唯品会样本需要网页脚本。具体支持范围见 [链接导入](link-import.md)。独立衣橱还通过浏览器导入一件 tentree 白色 T 恤，完成真实 CPU 去背景、Codex 自动填写、来源备注保存及确认后持久化。

得物完整分享文字在真实页面中解析出正确的 Polo 衫标题及三张商品图。选择正面图导入后，完成真实去背景与 Codex 识别，返回深灰色／白色、翻领、短袖、宽松等字段，品牌与价格保持未填。确认保存后刷新并检查存储，来源保留商品与款式编号，分享追踪参数已过滤；后台无遗留识别任务。得物桌面二维码页和旧版跳转页面仍可能缺少商品图，成功范围限于已验证的公开页面结构。

网页采集接收流程通过真实浏览器输入合成 PNG 数据，验证解析、图片预览、选择导入、逐次关闭 AI 和去背景、来源保留及确认保存。扩展下载端点返回有效 ZIP，八个文件与源码逐字节一致。扩展权限、可见图片矩形、截图裁切、选择复制及前端协议通过模拟 Chrome API 测试；本轮未在实际安装的 Chrome / Edge 扩展中完成运行验收。

参考价格完成得物实际商品验收：预览显示 ¥399／发售价格，独立来源与抓取时间随图片导入，购买金额初始为空。用户点击“用作购入价”后，表单填入金额和人民币；确认保存并刷新后仍保留 399.00 购买价及独立参考价。自动检查覆盖参考价不计入未确认花费、购买金额独立修改、来源字段不可由普通编辑伪造，以及备份恢复验证。

## 界面检查

服饰主信息与末尾“详情”在 390 × 844 和 1440 × 1000 视口完成操作检查，补充面板无横向溢出。主表单填写子分类、图案、材质、尺码，补充面板填写风格、合身度、版型、领型、袖长、长度及洗护说明；统一保存并刷新后内容一致。实际键盘 Escape 只关闭顶部弹窗，并保留主表单草稿、恢复触发按钮焦点。图片美化接口与 Codex 设置、预览操作通过界面检查，控制台未发现错误或警告。

以 390 × 844 与 1440 × 1000 的浏览器视口检查手机和电脑布局。核对底部导航、桌面侧栏、衣柜网格、中文动态洞察、弹窗 Escape 关闭与焦点返回、日历及打包流程。具体结果见 [界面验收](design-qa.md)。

助手连接页在这两个视口均无横向溢出；实际配对完成后显示 Codex、验证时间和授权到期时间。浏览器检查未发现连接页控制台错误或警告。

穿搭编辑器在 1440 × 1000 视口完成真实指针拖动、键盘移动、滑条缩放旋转、模式切换、保存及刷新后布局还原。在 390 × 844 视口完成拖动、真实规则推荐 6 件单品、选用后继续编辑与保存；来源保持为规则推荐，穿着次数保持不变。

固定保存栏、浅色背景图片混合显示、拼图从 5 件增至 6 件后重排、自由模式新增单品后保留变换，以及保存期间禁用编辑均通过真实浏览器检查。7 件单品保存并刷新后布局一致，手机与桌面无横向溢出，关闭编辑器后焦点返回。

最终构建在手机和桌面视口均完成直接拖动缩放与旋转手柄。桌面保存后的宽度 44.8845% 和角度 89.7176° 在刷新后准确还原；手机缩放至 53.7277% 并旋转至 44.4762°，中心位置不变，两个操作手柄和保存按钮可见。原测试衣柜与默认衣柜的数据摘要保持一致，本机技能安装文件与仓库源文件逐字节一致。

## 适用范围

当前为单用户本机应用。Claude Code 的接口与安装路径通过自动测试，未进行真实 Claude Code 会话验证。真实手机硬件、Safari、跨设备部署和公网多用户服务未在本轮验证。去背景质量依赖照片内容，复杂背景及细节应人工检查。
