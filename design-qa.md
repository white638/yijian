# 穿搭编辑器设计验收

版本：0.2.0。检查日期：2026-09-10。

## 视觉依据

参考为 Acloset 穿搭编辑器的手机截图：`7547261d9acf5c2dcfdb85b21a9a6911.jpg`（创建方式）、`b95bfa6ee529e56e16deb91377069776.jpg`（画布）、`9c27436f3022393ed802b56215620dc8.jpg`（分类）、`d347bf2de835c9a0f97d4ff47264b94d.jpg`（拼图）。原图为 1272 × 2772 像素；比较范围为应用内容区域，系统状态栏不属于实现目标。

实现位于本机独立测试服务的穿搭页。浏览器截图使用 390 × 844 和 1440 × 1000 CSS 视口，截图像素尺寸与视口一致。移动参考按内容比例比较信息层级、操作和图片排列；桌面使用画布与衣柜并排的适配布局。素材、单品数量及品牌视觉不同，不做像素级复刻判断。

参考图与手机实际渲染截图在同一组图像输入中完成对照，并检查电脑截图。局部检查覆盖创建方式、选中衣物边界、浅色画布图片和保存入口。截图保存在本次浏览器验收记录中，未导出为仓库文件。

## 视觉与操作

| 检查面 | 结果 |
| --- | --- |
| 字体与层级 | 沿用衣间中文界面的字体、粗细与辅助文字颜色；入口标题和说明分层，手机不截断操作名称。 |
| 间距与布局 | 手机两列方式入口、纵向画布与衣柜；桌面四列入口与双栏编辑。两个视口无页面级横向溢出。 |
| 颜色与状态 | 使用既有浅紫选中态、浅色画布和深色保存按钮，选中、禁用和焦点状态可区分。 |
| 图片 | 商品图按比例包含在单品框内，浅色背景混合显示没有明显白色矩形边缘；没有照片的记录显示类别图标。 |
| 文案与内容 | 四种方式、分类、操作说明及状态使用中文。规则推荐明确标记来源，购买价与参考价保持独立。 |

保存后的坐标、尺寸、角度、图层和背景可恢复；模板重排、自由位置保留、配饰展示、保存期间禁用、Escape 关闭和焦点返回已完成实际浏览器检查。浏览器控制台未观察到错误或警告。

选中衣物后显示独立的缩放和旋转手柄，触摸区域为 44 × 44 px。电脑端实际拖动将宽度从 28.6667% 调整为 44.8845%，旋转至 89.7176°；保存、刷新并重新打开后数值一致。手机端实际拖动将宽度从 70% 调整为 53.7277%，旋转至 44.4762°，中心位置保持不变。手柄、选中边框与底部保存入口在手机截图中清晰可见，页面宽度与滚动宽度均为 390 px。

实际手机硬件和 Safari 尚未验证；自动检查覆盖范围见 [验证记录](docs/validation.md)。

final result: passed
